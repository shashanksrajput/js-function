// Case 1 (the element is found in the array by taking its position).
let arr = [2, 3, 8, 7, 9, 6];

      //the array which are present in given index.
      console.log(arr[3]);

    

// Case 2 (the element didn't find is return as -1) .
function evenodd(n) {
   if (n%2==0) {

    return true
   }

  else return-1; 
   }  
console.log(evenodd(3));